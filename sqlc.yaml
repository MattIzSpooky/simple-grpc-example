version: "1"
packages:
  - name: "db"
    path: "internal/db"
    engine: "postgresql"
    schema: "migrations"
    queries: "queries"
    emit_json_tags: true
    emit_prepared_queries: false
    emit_interface: false
    emit_exact_table_names: false
